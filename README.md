# Weather CLI

Returns a three day forecast for a given city, enjoy

## Usage

```bash
go build weather.go

./weather -c "Kalmar"
```

For ease of use:

```bash
ln -s ./weather $HOME/.local/bin/weather
```
